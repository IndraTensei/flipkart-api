name: flipkart-scraper-api
image: flipkart-scraper-api
command: npm start
securityContext:
  capabilities:
    add:
      - NET_BIND_SERVICE
containers:
  - image: flipkart-scraper-api
    ports:
      - 3000:3000
volumes:
  - /app/node_modules
